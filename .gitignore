# Node / JS
node_modules
bower_components
*.js.map

# Yarn (Berry / v2+)
.yarn/cache/
.yarn/unplugged/
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*
yarn-error.log

# Lockfiles (this repo uses Yarn)
package-lock.json

build/
dist/
