{
  "name": "switchyomega",
  "version": "2.5.21",
  "description": "Proxy configuration tool",
  "scripts": {
    "build": "tsc && cp -r omega-target-chromium-extension/overlay/* dist/",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "typescript": "^5.3.3"
  }
} 